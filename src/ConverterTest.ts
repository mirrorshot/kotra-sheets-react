import React from 'react';
import { render, screen } from '@testing-library/react';
import Converter from "./Converter";
import Job from "./sheets/Job";

test('convert OneShot to string', () => {
    // render(<Job />);
    // const sheet = screen.getByText(/Job/);
    // let compressed = Converter.compress(sheet);
    // console.log('compressed data: ', compressed);
    // let decompressed = Converter.extract(compressed);
    // console.log('decompressed: ', decompressed);
    // expect(decompressed).toEqual(sheet);
});
